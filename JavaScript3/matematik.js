function karesi(x){
    //return x*x;
    return Math.pow(x,2);
}

function kubu(x){
    //return x*x*x;
    return Math.pow(x,3);
}

function dorduncu(x){
    //x*x*x*x yani x üzeri 4
    return Math.pow(x,4);
}

function besinci(x, y){
    //x*x*x*x yani x üzeri 4
    return Math.pow(x, y);
}

function faktoryel(x){

}
